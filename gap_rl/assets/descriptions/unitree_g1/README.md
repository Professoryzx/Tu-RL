# Unitree G1 dual-arm description

Place the official Unitree G1 dual-arm URDF and supporting meshes in this
folder. The default configuration expects a file named
`unitree_g1_dualarm.urdf` alongside the mesh assets provided by Unitree.
